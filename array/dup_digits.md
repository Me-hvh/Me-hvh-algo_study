# 存在重复元素

**Difficulty:** Easy

**Tags:** 数组, 哈希表, 排序

## 题目描述

给你一个整数数组 ``nums`` 。如果任一值在数组中出现 至少两次 ，返回 ``true`` ；如果数组中每个元素互不相同，返回 ``false`` 。

## 示例 1：
```
输入：nums = [1,2,3,1]
输出：true
```
**解释:** 元素 ``1`` 在下标 ``0`` 和 ``3`` 出现。

## 提示：
- `1 <= nums.length <= 10^5`
- `-10^9 <= nums[i] <= 10^9`

